package com.leetcode.tree.binarysearchtree;

import com.datastructure.tree.model.BinaryTreeNode;

/**
 * LeetCode: 450 (M)
 * Given a root node reference of a BST and a key, delete the node with the given key in the BST. Return the root node reference (possibly updated) of the BST
 */
public class _Delete_Node {

    /*
    Case 1: Node to be deleted is a leaf node -- return null to its parent.
    Case 2: Node to be deleted does not have a left child -- return right child to its parent.
    Case 3: Node to be deleted does not have a right child -- return left child to its parent.
     */
    private static BinaryTreeNode deleteNode(BinaryTreeNode root, int key) {

        return null;
    }

    public static void main(String[] args) {

    }
}
